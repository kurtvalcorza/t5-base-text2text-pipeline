# Release status

Current status: **Candidate** — E2E fine-tuning tutorial source complete, awaiting supported clean-runtime execution. The pipeline package (inference contract, digest-pinned SciTLDR-A abstract–title corpus, ROUGE-1/2/L with a Lead baseline, bounded decoder fine-tuning that teaches a caller-chosen prefix, safetensors adapter), 33 offline unit tests plus 5 notebook-parity tests and 2 model-backed tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/t5_base_text2text_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

The earlier `TASK-INFERENCE` notebook had a recorded Kaggle CPU pass (blob `fbc9be0e4069` at `198685c`, 2026-09-14); that evidence is retained in `docs/release-verification.md` as history and does not carry over to the `E2E` notebook, which is a different blob with a different default path.

No licence or remote-code gate is open for DIMER hosting (Apache-2.0 weights; safetensors; `trust_remote_code=False`). The adaptation sample is fetched at run time from the pinned `allenai/scitldr` commit (SciTLDR-A, Apache-2.0, three JSON-Lines files, 5.5 MB) and is not redistributed by this repository. Two behaviours worth a reviewer's eye: the frozen model answers the new prefix with one-word outputs (`True` / `False`), so its frozen score is near zero by construction and the meaningful reference points are the Lead-1 baseline and the frozen `summarize: ` prefix; and the adapter changes the decoder blocks every prefix shares, so the trained prefixes are affected too — the notebook re-generates one German translation after adaptation and the card, notebook and README say so.
