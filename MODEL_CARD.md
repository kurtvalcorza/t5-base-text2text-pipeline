---
license: apache-2.0
model_card_spec: "1.1"
pipeline_tag: text2text-generation
task: "Others - Text-to-Text Generation"
base_model: google-t5/t5-base
date_published: "2019-10"
date_published_source: "google-research/text-to-text-transfer-transformer initial release 2019-10-23 (arXiv:1910.10683 v1 same day); Hub history begins 2019-12-12"
---

# T5-Base — Text-to-Text Transfer Transformer (Text2Text Generation)

[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-google--t5%2Ft5--base-ffcc4d?style=flat)](https://huggingface.co/google-t5/t5-base)
[![Upstream GitHub](https://img.shields.io/badge/Upstream%20GitHub-google--research%2Ftext--to--text--transfer--transformer-181717?style=flat&logo=github&logoColor=white)](https://github.com/google-research/text-to-text-transfer-transformer)
[![arXiv Paper](https://img.shields.io/badge/arXiv-1910.10683-b31b1b.svg)](https://arxiv.org/abs/1910.10683)
[![License: Apache-2.0](https://img.shields.io/badge/License-Apache--2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)

> [!WARNING]
> ⚠️ **Provided for research, training, and evaluation purposes only.** Model weights are redistributed unmodified under their upstream license, which controls your use, including any commercial use or redistribution; the accompanying code and notebooks are released under this repository's license. All of it is supplied **"as is"**, without warranty of any kind, and has not been validated for production, clinical, or safety-critical use. Running the notebooks downloads third-party weights and datasets governed by their own licenses and consumes compute on your own Colab/Kaggle account. To the maximum extent permitted by law, the maintainers of this repository and the DIMER platform accept no liability for any damages arising from their use. Hosting implies no affiliation with or endorsement by the original authors.

---

## Interactive Colab Tutorials

This pipeline provides a ready-to-run interactive Google Colab notebook that exercises the repository's public API end to end — bootstrap a fresh runtime, stage and verify the pinned upstream revision, fetch and validate a digest-pinned referenced corpus, score the frozen model under a prefix it has never seen beside a Lead baseline and its nearest trained prefix, teach the prefix with a bounded fine-tuning of the last decoder blocks, evaluate on an independent test split, generate for unseen abstracts, and export a reloadable adapter:

- **End-to-End Fine-Tuning Tutorial**:  
  [![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/t5-base-text2text-pipeline/blob/main/tutorials/t5_base_text2text_colab.ipynb) [`t5_base_text2text_colab.ipynb`](https://github.com/kurtvalcorza/t5-base-text2text-pipeline/blob/main/tutorials/t5_base_text2text_colab.ipynb)  
  *Caller-prefixed text-to-text generation and bounded supervised fine-tuning with the pinned `google-t5/t5-base` weights: `generate` under the trained prefixes (`translate English to German: `, `summarize: `); 300 / 50 / 100 SciTLDR-A abstract–title pairs (Apache-2.0, three pinned JSON-Lines files) are validated and split without leakage; the frozen model is scored under the **new prefix** `paper title: ` (ROUGE-L 0.71 — an unknown prefix is just words) beside the Lead-1 baseline (14.08) and its own `summarize: ` prefix (18.99); the last four decoder blocks (37,757,952 of 222,903,552 parameters) are fine-tuned for two epochs with validation-ROUGE-L epoch selection; the adapted model is scored on the held-out split (ROUGE-L 29.97 at 6.5 words in the recorded run), titles four unseen abstracts, and is exported as a 151 MB safetensors adapter whose reload reproduces every output.*

---

#### Description

`google-t5/t5-base` is the 220-million-parameter checkpoint of the original Text-To-Text Transfer Transformer released by Raffel et al. (JMLR 21(140), 2020; arXiv:1910.10683), pinned here to revision `a9723ea7f1b39c1eae772870f3b547bf6ef7e6c1`. It is a standard encoder-decoder Transformer: 12 encoder and 12 decoder layers, `d_model` 768, 12 attention heads of width 64, feed-forward width 3072, relative position biases with 32 buckets instead of absolute position embeddings, and a shared SentencePiece vocabulary of 32,128 pieces (`spiece.model`, byte-identical to T5-Small's, 100 of them sentinel `<extra_id_N>` tokens) — all read from the snapshot `config.json` and `tokenizer.json` (this snapshot ships no `tokenizer_config.json`; `T5TokenizerFast` loads from `tokenizer.json` with its class defaults, and the smoke run confirmed pad 0 / EOS 1 decoding). The checkpoint was pre-trained with a span-corruption denoising objective on C4 and simultaneously on a supervised multi-task mixture in which every task is cast as text in, text out, with a plain-language prefix naming the task (upstream README, "Training Details"); this is the pre-FLAN T5, not an instruction-tuned model. At inference the encoder reads the prefixed input once, then the decoder emits one SentencePiece token per step, starting from the pad token (`decoder_start_token_id` 0) and stopping at `</s>` (id 1) or a step ceiling; nothing is adapted, fine-tuned or conditioned beyond the text the caller supplies. What this repository adds is packaging and a bounded adaptation contract: the `T5BaseText2TextPipeline` class in `src/t5_base_text2text_pipeline/pipeline.py`, digest verification of the local snapshot (`verify_snapshot`, `stage_missing_files`), input validation with named ceilings, a fixed output contract, and no task prefixes of its own — the caller writes `summarize: ` or `translate English to German: ` in front of the text — plus reference-based corpus metrics (`metrics.py`: ROUGE-1/2/L F1, own implementation) with a Lead-N baseline, a digest-pinned real referenced corpus (`samples.py`: SciTLDR-A abstracts paired with their paper titles, validated and split without leakage), supervised fine-tuning of the last *k* decoder blocks (`adapt`, default 4 of 12) that teaches a caller-chosen prefix with validation-ROUGE-L epoch selection, and a safetensors adapter (`save_artifact` / `from_artifact`) that carries only the trained tensors, records the prefix it was trained for, and is bound to the base weight digest.

#### Intended Use and Limitations

###### Primary Intended Uses

The task is conditional text generation: input one UTF-8 string that begins with a task prefix the checkpoint was trained on, output one generated string plus its token count and the generation settings used. The upstream `config.json` names four such prefixes in `task_specific_params` — `summarize: `, `translate English to German: `, `translate English to French: `, `translate English to Romanian: ` — and the pipeline exposes the same list as `TASK_PREFIXES`, echoing which one (if any) the input started with as `known_prefix`. Envisioned applications are single-document abstractive-leaning summarisation of short English passages and English-to-German/French/Romanian sentence translation in research prototypes, teaching material, and as a mid-size deterministic baseline against which larger seq2seq or instruction-tuned models are compared. In a larger system the pipeline is an inference component that produces candidate text for a human or a downstream scorer; it is the second-smallest member of the T5 family and is chosen where an 892 MB float32 weight file and roughly 2–3× the T5-Small CPU latency (measured below) are acceptable in exchange for the larger model (the `t5-small-text2text-pipeline` sibling is the 60 M-parameter step down; which one produces better text on your documents is something only your own references can settle). The second intended use is teaching a new prefix: given 8–20,000 `{id, source, targets}` input–output records from the operator's own task and a prefix string of their choosing, `adapt` prepends the prefix, fine-tunes the last four decoder blocks (37,757,952 parameters, about 17 % of the model) for a few epochs, selects the epoch by validation ROUGE-L under the operator's generation settings, and `save_artifact` exports the trained tensors as a 151 MB safetensors adapter that `from_artifact` applies on top of the digest-verified base — the recorded run takes `paper title: ` from a ROUGE-L of 0.71 (one-word answers to an unknown prefix) to 29.97 on held-out abstracts, above the Lead-1 baseline (14.08) and the frozen `summarize: ` prefix (18.99), in about 5 minutes of CPU training.

###### Primary Intended Users

The intended users are machine-learning engineers, NLP researchers and application developers integrating a seq2seq baseline into research prototypes, internal tooling, or the DIMER model workbench, and teaching it a task of their own for which they hold a small referenced corpus. The pipeline assumes its users understand that the model follows only the trained prefixes and not free-form instructions until a prefix is taught, that greedy decoding returns one deterministic candidate rather than a probability over outputs, that generated text can be fluent and wrong, that the model reads at most 512 SentencePiece tokens of input, that ROUGE measures n-gram overlap with references — moved by output length as much as by content — and is neither faithfulness nor a human judgement, and that an adapter trained on the shared decoder blocks changes every prefix the model knows, not only the one it was taught. It is not designed for hobbyist chat, for translating documents wholesale without segmentation, or for any language pair the checkpoint was not trained on.

###### Out-of-scope use cases

1. **Capability boundary:** not an instruction-following or chat model — prompts other than the four trained prefixes (questions, "rewrite this", other language pairs, other source languages) produce copies of the input, fragments, or unrelated text; not a classifier, embedder or masked-LM (the GLUE/SuperGLUE tasks in its training mixture are not exposed here); not multilingual beyond English source text with German, French or Romanian targets (upstream README `language` list). For fill-mask or sentence embeddings use `bert-masked-lm-pipeline`; for open-ended generation use `gpt2-text-generation-pipeline`. Not a general training framework: `adapt` fine-tunes only whole decoder blocks (1–12 of them, the last *k*), with AdamW, a fixed learning rate, no scheduler, no mixed precision, no gradient accumulation and no early stopping beyond epoch selection; the encoder, the embeddings, the tied output projection and the vocabulary are never modified, so it cannot teach the model a new language, lengthen its input window, or add a prefix that needs the encoder to read the input differently.
2. **Input boundary:** only `str` is accepted (`TypeError` otherwise); empty or whitespace-only text, text over `MAX_TEXT_CHARS = 20000` characters, or text that tokenises to more than `MAX_INPUT_TOKENS = 512` pieces including `</s>` is rejected with `ValueError` — the pipeline refuses rather than silently truncating; `max_new_tokens` is capped at `MAX_NEW_TOKENS = 512` and `num_beams` at `MAX_NUM_BEAMS = 8`; one input per call, no batching. Datasets for `validate_dataset`, `evaluate` and `adapt` are lists of `{id, source, targets}` records with sources **without** a prefix (`{id, source, target}` with one string is normalised): fewer than `MIN_RECORDS = 8` or more than `MAX_RECORDS = 20000` records, a duplicate or malformed `id` (pattern `^[A-Za-z0-9_.:-]{1,64}$`), an empty source or one over `MAX_TEXT_CHARS`, an empty reference list, an empty reference or one over `MAX_TARGET_CHARS = 400` characters, or a record that is not a mapping is rejected with `ValueError`; the prefix must be a non-empty string of at most `MAX_PREFIX_CHARS = 64` characters ending in `: `; `evaluate` accepts 1–`MAX_EVAL_RECORDS = 2000` records and applies the inference ceilings to every prefixed source (over 512 tokens is refused, not cut); `adapt` requires at least `MIN_RECORDS` training records and refuses `epochs` outside 1..20, `batch_size` outside 1..32, `lr` outside (0, 1e-2] or `trainable_decoder_layers` outside 1..12. Training sources are truncated to `MAX_TRAIN_SOURCE_TOKENS = 512` and targets to `MAX_TRAIN_TARGET_TOKENS = 64` tokens (the only truncation in the repository, and it applies to training only). `load_artifact` refuses an adapter whose `format`, `base_model` id, revision or `weight_sha256` differ from the constants, whose tensor names are not `decoder.block.*`, or whose file digest differs from its manifest.
3. **Decision boundary:** not for producing text that is acted on without a human reading it — contract or medical translation, summaries that feed automated decisions, content published under a person's name — and not for any use where a fabricated sentence causes harm before it is checked. A ROUGE gain on the operator's validation split is not a release decision: it is a single seeded split of one corpus with no dispersion estimate, ROUGE says nothing about faithfulness, and an adapter that teaches one prefix has changed the shared decoder blocks every other prefix uses.

#### Factors

###### Groups

The pipeline is human-adjacent rather than human-centric: it consumes and emits natural-language text that routinely mentions people, but it carries no demographic labels and its outputs are strings, not decisions about a person. Its pre-training corpus C4 is filtered English web text whose demographic composition the upstream authors did not audit; the pinned upstream README answers "Bias, Risks, and Limitations" with "More information needed", and Dodge et al. (2021) document that C4's block-list filtering disproportionately removed text about and by minority groups. Neither the upstream card nor this repository reports any group-level quality breakdown for summaries or translations (for example by dialect, named-entity origin, or gendered referents in German/French/Romanian output, all of which require grammatical gender the English source may not specify). The fairness audit therefore transfers to the operator: before deployment, score outputs on a labelled sample of your own inputs stratified by the groups that matter to your application, and treat systematic gender defaulting or degraded quality on a group as a blocker.

###### Instrumentation

There is no physical sensor: the training data was produced by software. C4 was built from the April 2019 Common Crawl web snapshot by rule-based filtering — keeping lines ending in terminal punctuation, dropping pages with fewer than five sentences, pages containing words from a block list, pages with placeholder or code-like text, and duplicate three-sentence spans (paper §2.2) — and the supervised tasks were assembled from published benchmark datasets listed in the upstream README (CoLA, SST-2, MRPC, STS-B, QQP, MNLI, QNLI, RTE, CB, COPA, WiC, MultiRC, ReCoRD, BoolQ) plus the translation and summarisation corpora behind the four config prefixes. The instrument characteristics that reach the model are text encoding and tokenisation: the pipeline lower-cases nothing, so casing, punctuation, Unicode normalisation, HTML residue and line breaks in the caller's input become SentencePiece tokens and shift the output; the vocabulary was fit on C4 English (with some German, French and Romanian), so characters outside it become `<unk>` (id 2) and vanish from the output. The pipeline does not detect encoding damage, boilerplate, or a change in input domain; it only rejects the type, length and token-count violations listed above.

###### Environment

Operating environment: Python 3.12 with `torch==2.14.0`, `torchvision==0.29.0`, `torchaudio==2.11.0`, `transformers==4.57.6`, `tokenizers==0.22.2`, `sentencepiece==0.2.2` (exact pins in `pyproject.toml`); `from_pretrained` picks `cuda:0` when available, else CPU, and loads the weights in float32 on both. Measured on this repository's smoke run (Windows venv `dimer-next16`, `CUDA_VISIBLE_DEVICES=-1`, `device="cpu"` passed explicitly, Intel Core Ultra 9 275HX): loading and digest-verifying the 894 MB snapshot took 4.55 s; `translate English to German: The house is wonderful.` (11 input tokens) generated 5 tokens in 0.29 s greedy and 0.21 s with `num_beams=4`, both yielding `Das Haus ist wunderbar.`; a 94-token `summarize: ` input (`max_new_tokens=48`) generated 40 tokens in 0.83 s and stopped at `</s>` with a two-sentence output. The same three calls through the `t5-small-text2text-pipeline` sibling on the same CPU took 0.16 s, 0.31 s and 0.09 s (T5-Base is 1.8×, 2.7× and 2.3× slower) after a 3.89 s load of its 244 MB snapshot; per generated token the summarisation step cost 21 ms here against 6.5 ms for T5-Small. Adaptation on the same machine, from the committed notebook's recorded run: reading the three pinned SciTLDR-A files (5.5 MB) from the cache and drawing 300 / 50 / 100 abstract–title records took under a second; 4-beam generation with `max_new_tokens` 24 took 0.3–0.6 s per abstract, so scoring the frozen model on the 100-abstract test split took 26.8 s under `paper title: ` (ROUGE-1/2/L 0.71 / 0.67 / 0.71 at 1.0 words — one-word answers such as `True` and `False` to an unknown prefix) and 64.9 s under `summarize: ` (21.68 / 8.53 / 18.99 at 16.7 words; Lead-1 16.73 / 4.83 / 14.08; titles average 7.4 words); two epochs of AdamW over 300 prefixed abstracts at batch 8 with the last four decoder blocks trainable (37,757,952 of 222,903,552 parameters, `lr=5e-4`, weight decay 0.01, gradient clipping 1.0, seed 0) took 284.2 s including the per-epoch validation scoring (validation ROUGE-L 0.73 frozen → 33.11 → 35.32; epoch 2 selected); re-scoring the test split took 51.3 s (ROUGE-1/2/L 31.61 / 16.40 / 29.97 at 6.5 words); the adapter is 151,037,976 bytes over 52 tensors, and the reloaded pipeline reproduced 6/6 outputs byte for byte. Counter-examples recorded during the build on the same split: two decoder blocks at `lr=3e-4` for two epochs reached ROUGE-L 16.11 and still answered `False` for some abstracts; re-targeting the trained `summarize: ` prefix to titles with four blocks for three epochs reached 28.89 — no better than teaching the new prefix. CUDA and the Hub-download path were not executed. Data environment: inputs are assumed to be well-formed English prose of the kind found on the filtered web (news, encyclopaedic, expository text) and, for translation, single sentences; summaries of text much shorter than a paragraph degenerate into near-copies, inputs above 512 tokens are refused, and text far from web English (code, tables, transcripts, other languages) yields output the pipeline cannot flag as degraded.

#### Metrics

###### Performance Measures

Three reference-based performance measures, implemented in `src/t5_base_text2text_pipeline/metrics.py` with no external scorer: corpus **ROUGE-1**, **ROUGE-2** and **ROUGE-L** F1 in percent — lower-cased runs of ASCII letters and digits as tokens, no stemming, unigram/bigram overlap F1 and longest-common-subsequence F1 over the whole output, the best score over an item's references, averaged over items; rouge-score-style, not rouge-score-identical. `text_metrics(hypotheses, references)` returns them with `mean_output_words` and `mean_reference_words`, because ROUGE moves with output length as much as with content; `pipe.evaluate(records, prefix=..., max_new_tokens=..., num_beams=...)` prepends the prefix, generates for a validated dataset and returns them with `known_prefix` (whether the prefix is a trained one), `hit_token_ceiling`, the settings, the verdict `measured` (≥ `MIN_SCORED_RECORDS = 50` records) or `measured-small-sample`, the elapsed time and the model identity; `lead_baseline(records, n_sentences)` scores the first N sentences of each source as its output. ROUGE-L selects the epoch in `adapt`. `generate` itself still emits free text plus counts and flags (`text`, `generated_tokens`, `input_tokens`, `stopped_by`, `known_prefix`), none of which is a quality score, and no faithfulness or factuality measure ships; translation quality would need the caller's own BLEU/chrF. The public per-item `evaluation_report(result, references)` helper scores one output against its references as `sample-sanity` and still returns `not-measurable` with an empty `metrics` list when no reference is supplied. Upstream's Table 14 figures (ROUGE on CNN/DailyMail, BLEU on WMT) are upstream claims, not reproduced here, and are not comparable to the title figures below — different task, different references. Recorded values on the seeded SciTLDR-A test split (100 abstracts, `max_new_tokens` 24, 4 beams, titles averaging 7.4 words): Lead-1 16.73 / 4.83 / 14.08; frozen under `summarize: ` 21.68 / 8.53 / 18.99 at 16.7 words; frozen under `paper title: ` 0.71 / 0.67 / 0.71 at 1.0 words; adapted under `paper title: ` (last four decoder blocks, best of two epochs) 31.61 / 16.40 / 29.97 at 6.5 words; four unseen abstracts after adaptation with a `measured-small-sample` verdict.

###### Decision thresholds

The default decision rule is greedy decoding: at every step the token with the highest logit is emitted (`num_beams=1`, `do_sample=False`), exposed as `DECISION_RULE` and reported in `generation.decision_rule`; this is an implicit argmax threshold with no minimum-probability cut-off, so the model always produces some token. `num_beams` up to 8 replaces the per-step argmax with a beam search over whole sequences. Generation stops at `</s>` or at `max_new_tokens` (default `DEFAULT_MAX_NEW_TOKENS = 64`, ceiling 512). The upstream `task_specific_params` block also carries `min_length`, `length_penalty`, `no_repeat_ngram_size` and `early_stopping` values for each prefix; the pipeline does **not** apply them — `model.generate` reads only the snapshot's `generation_config.json`, which sets nothing but the start, pad and EOS ids — so a caller who wants the upstream summarisation settings (`num_beams=4`, `min_length=30`, `no_repeat_ngram_size=3`) gets only `num_beams` from this interface. No acceptance threshold on output quality is shipped because the model emits no confidence: the trade-off the operator sets is between over-long or repetitive output (raise `num_beams`, lower `max_new_tokens`) and truncated output (raise `max_new_tokens`), judged on their own references. The one selection rule in the adaptation path is explicit: `adapt` keeps the epoch with the highest validation ROUGE-L under the prefix and generation settings the caller passes (epoch 0 is the frozen model, so a run that never improves returns the base weights unchanged) and reports `best_epoch` and the per-epoch history; whether the resulting gain on the test split is worth deploying — and whether the change to the shared decoder blocks is acceptable for the trained prefixes — is the operator's decision and no threshold is shipped for it.

###### Approaches to uncertainty and variability

Every reported corpus metric is one value on one seeded split of one corpus: `build_sample_dataset(seed=42)` fixes the 300 / 50 / 100 draw from SciTLDR-A's own train/dev/test members, `adapt(seed=0)` fixes the shuffle order, and no repetition over seeds or splits is performed, so no confidence interval or standard deviation is available and none is claimed — the +29.27 ROUGE-L gain on the test split is a point estimate whose spread is unknown, and the four-abstract `measured-small-sample` verdict says so explicitly. When references are supplied to the per-item `evaluation_report`, it computes ROUGE for exactly one item and says so — no dispersion exists for one observation, and the verdict is `sample-sanity`, never a quality measurement. The upstream Table 14 figures are single evaluations by the upstream authors with no reported interval. Inference is deterministic for a given input, settings, weights, device and library versions: sampling is disabled (`do_sample=False`), dropout is off under `model.eval()`, and no seed is needed — the smoke run produced the same German sentence under greedy and 4-beam decoding. Beam search and greedy decoding can still differ from each other, and float32 kernel differences between CPU and GPU can flip near-tied tokens and change the rest of the sequence from that point on. The pipeline emits no probability or confidence at all: `generated_tokens` and `stopped_by` are counts, not scores. A caller who needs calibrated confidence in an output must compute it themselves (for example, sequence log-probabilities via `transformers` `output_scores`, then a calibration map fit on their own labelled data); nothing in this repository does so. Training is deterministic on one CPU for one seed and library set but is not bit-reproducible across devices or `torch` builds.

#### Ethical considerations and biases

###### Data

Upstream discloses that the checkpoint was pre-trained on C4 (the Colossal Clean Crawled Corpus, filtered Common Crawl web text) and Wiki-DPR for the unsupervised objective and on the named GLUE/SuperGLUE datasets for the supervised mixture (pinned upstream README, "Training Data"), with the translation and summarisation corpora behind the four prefixes described in the paper; disclosure stops there — no per-document licensing, consent status or personal-data audit is given for C4, and web crawl text is known to contain names, contact details and other personal data of identifiable people, so the presence of personal data is not ruled out and should be assumed. This repository distributes code, tests and documentation; the 892 MB `model.safetensors` and the tokenizer files are git-ignored and staged locally under `weights/t5-base/` with a manifest. The adaptation sample is not shipped either: `fetch_corpus` downloads three pinned objects at run time — `train.jsonl`, `dev.jsonl` and `test.jsonl` of SciTLDR-A (3,155,015 / 1,124,865 / 1,204,107 bytes; SHA-256 `b222771d…` / `3191fa98…` / `fb42dd6c…`) from `raw.githubusercontent.com` at the pinned `allenai/scitldr` commit `5ccad9c0…`: 3,229 computer-science paper abstracts with their titles under Apache-2.0 (Cachola et al., 2020) — refuses any other bytes, and caches them under `weights/scitldr/` (git-ignored). The abstracts are public OpenReview submissions and name their authors' methods and claims; the sample is used only to fit and score the last four decoder blocks, and the 300 / 50 / 100 subset is reproducible from the digests and `SAMPLE_SEED = 42`. An adapter trained on the sample is a derived work of the Apache-2.0 weights and of the corpus. Nothing in this corpus is a substitute for the operator's own task: BYOD records pass through the same validation and are written to `outputs/…_train.csv` in the notebook, so an internal document set with its reference outputs is restricted data the moment it is loaded. The operator must audit the text they submit or train on for personal, confidential or proprietary content; the pipeline performs no such check and keeps no log.

###### Human Life

The pipeline is not intended for decisions in health, safety, criminal justice, employment, credit, housing or any other domain central to human life, and it has not been validated or certified for any of them by anyone. Its only validation is the offline unit suite (33 tests, plus 2 model-backed tests that run when the snapshot is staged) and the CPU runs recorded in this repository on synthetic sentences and paper abstracts. Where a sensitive use is foreseeable — summarising clinical notes, translating a legal notice, condensing a job application, or teaching a prefix on such material — it is admissible only with a human reading the generated text against the source before it is used, an independent evaluation on representative documents with references, and whatever regulatory clearance the domain requires.

###### Mitigations

Implemented and inspectable in `src/t5_base_text2text_pipeline/pipeline.py`: (1) supply chain — `MODEL_REVISION` is a 40-hex commit; `stage_missing_files` refuses a manifest whose `modelId`/`revision` differ from the constants and fetches absent files only with `allow_download=True`, one file at a time at that revision; `verify_snapshot` re-hashes every file in `weights/t5-base/dimer-base-manifest.json` and raises on the first size or SHA-256 mismatch before any weight is loaded; loading is `local_files_only=True` from the verified directory, or from the Hub with `revision=MODEL_REVISION` only when explicitly allowed; `trust_remote_code=False` always. (2) Input integrity — `_validate` rejects non-`str`, empty, over-length (`MAX_TEXT_CHARS`) and over-token (`MAX_INPUT_TOKENS`, counted with the real tokenizer) inputs and out-of-range `max_new_tokens`/`num_beams` before the model runs. The public `validate_inputs()` helper applies the same checks through the same private `_check_inputs()` function and returns an input manifest (schema, ceilings, per-input observations including `known_prefix`, verdict, findings), so a caller can record exactly what was accepted or rejected without duplicating the validation logic; the token ceiling stays inside the pipeline because it needs the loaded tokenizer. (3) Reproducibility — exact `==` dependency pins, `model.eval()`, float32, deterministic decoding, and `model_id`/`model_revision`/`generation` in every result. (4) Refusals — no sampling, no batching, no prefix injection at inference; a missing snapshot with `allow_download=False` raises `FileNotFoundError`. (5) Dataset integrity — `fetch_corpus` verifies the byte count and SHA-256 of each pinned file before it is parsed; `validate_dataset` enforces the record schema, the id pattern, the size bounds and the character ceilings on sources and references and returns a content digest; `_check_prefix` bounds the prefix; `check_split_disjoint` fails on any source shared between splits; `split_dataset` de-duplicates sources and is seeded. (6) Training and artifact integrity — `adapt` trains only whole decoder blocks under an explicit `trainable_decoder_layers` bound, restores the best-epoch weights so a failed run leaves the base model, and truncates training sources at 512 and targets at 64 tokens (the only truncation, training only); `save_artifact` writes a safetensors file of the trained tensors plus a manifest carrying the format tag, the base model id, revision and weight digest, the prefix, the tensor list and the file SHA-256; `load_artifact` refuses any mismatch in the base fields, any tensor outside `decoder.block.*`, and any digest drift, so an adapter cannot be applied to a different base or altered in transit without failing. No statistical mitigation (data re-balancing, output filtering) is applied: the base weights are redistributed unmodified and the adapter learns whatever references the operator validates.

###### Risks and harms

Fabrication: the decoder produces fluent text whether or not it is entailed by the input — a translation can add or drop a negation, a summary can state a fact the source never made — and the reader who trusts it bears the harm; likelihood rises with input length and with distance from web-English news prose. Prefix misuse: an unrecognised prefix returns something rather than an error (`known_prefix` is `None`, not a refusal), so a caller who mistypes a prefix gets plausible-looking garbage. Bias amplification: C4's filtering and composition (Dodge et al., 2021) and gendered target languages mean summaries and translations can default to majority phrasing and masculine forms; data subjects and third parties bear that harm when the text is published. Adaptation drift: an adapter that teaches one prefix changes the decoder blocks every prefix shares — the recorded run's German translation is generated again after adaptation so the effect is visible on one sentence, and nothing measures it further — and a corpus with unfaithful references teaches unfaithful outputs; the artifact records the prefix and the training configuration but not the texts, so a misapplied adapter is a supply-chain risk of the operator's own making — the base-digest binding stops it from loading on the wrong model, not on the wrong task. Automation bias: fluent output is checked less carefully than a rough one. Silent quality loss: text near the 512-token limit is summarised from a lopsided reading of the document, and `max_new_tokens` truncation cuts mid-sentence (the T5-Small sibling's smoke summary ran to the ceiling; T5-Base's stopped at `</s>` after 40 of 48 allowed tokens, but a shorter `max_new_tokens` would have cut it). Data exposure: submitted text is processed in memory only, but the operator's surrounding system may log it. Magnitude ranges from an awkward sentence to a materially false statement attributed to a source.

###### Use cases

The pipeline must not be used for surveillance, profiling or social scoring — for example summarising intercepted or scraped personal communications to characterise an individual — nor to generate text that is presented as a person's own words or as a faithful record of a document without disclosure. It must not support unlawful discrimination in employment, housing, credit, insurance, education or healthcare access, nor deceptive, manipulative or predatory applications such as mass-producing misleading summaries of public documents or fabricated "translations" of statements. Any use that violates the Apache-2.0 terms of the upstream weights or the DIMER deployment terms is prohibited. The developers identify no further prohibited use beyond these because the model's capability is limited to short prefixed seq2seq text.

## Immutable provenance

- Model: `google-t5/t5-base`
- Revision: `a9723ea7f1b39c1eae772870f3b547bf6ef7e6c1`
- Snapshot manifest: `weights/t5-base/dimer-base-manifest.json`, 6 files (no `tokenizer_config.json` upstream at this revision), `totalBytes` 893837231
- `model.safetensors` SHA-256: `a90903540cc02cbeb7ff9f823f1a80eb778c7e22426a0e620b01c77a5ec8f5b4` (891646390 bytes)
- `config.json` SHA-256: `46dd7cb62d29c81fb551e0ef1ea274c24a46ba441eeb948897706252933df033` (1208 bytes)
- `spiece.model` SHA-256: `d60acb128cf7b7f2536e8f38a5b18a05535c9e14c7a355904270e15b0945ea86` (791656 bytes; byte-identical to the `t5-small` snapshot's)
- Weight format: SafeTensors; loader `T5ForConditionalGeneration.from_pretrained(<verified dir>, dtype=torch.float32, local_files_only=True, trust_remote_code=False)` with `T5TokenizerFast` from the same directory (built from `tokenizer.json`; no `tokenizer_config.json` in this snapshot)

## Input/output contract

- `T5BaseText2TextPipeline.from_pretrained(device=None, weights_dir=None, allow_download=False)`
- `generate(text, *, max_new_tokens=64, num_beams=1)` — `text`: one `str`, non-empty, ≤ 20000 chars, ≤ 512 SentencePiece tokens incl. `</s>`, beginning with the caller's own task prefix; `max_new_tokens` 1–512; `num_beams` 1–8. Returns `{"text", "generated_tokens", "input_tokens", "stopped_by", "known_prefix", "generation": {"max_new_tokens", "num_beams", "do_sample", "decision_rule"}, "device", "source", "model_id", "model_revision"}`.
- `TASK_PREFIXES` — the four upstream prefixes from `config.json`; informational, never applied.
- `verify_snapshot(path=None)` — returns the manifest dict with `path`; raises `FileNotFoundError` / `ValueError`.
- `stage_missing_files(path=None, *, allow_download=False, downloader=None)` — returns the list of manifest entries fetched.
- `evaluate(records, *, prefix, max_new_tokens=64, num_beams=1)` — validated `{id, source, targets}` records (1–2,000) generated under `prefix` → `{n, rouge1, rouge2, rougeL, mean_output_words, mean_reference_words, definitions, prefix, known_prefix, hit_token_ceiling, generation, verdict, adapted, seconds, model_id, model_revision}`; `evaluation_report(result, references=None, *, sample_kind="synthetic")` — `sample-sanity` with single-item ROUGE-1/2/L when references are supplied, else `not-measurable`.
- `adapt(train, val, *, prefix, epochs=2, lr=5e-4, batch_size=8, trainable_decoder_layers=4, seed=0, progress=None, eval_generation=None)` — requires `from_pretrained`; `train` 8–20,000 records, `val` optional (1–2,000; without it the final epoch is kept); fine-tunes the last *k* decoder blocks (`k` 1–12; `epochs` 1–20, `batch_size` 1–32, `lr` (0, 1e-2]) with AdamW and keeps the epoch with the highest validation ROUGE-L under `eval_generation`; returns `{prefix, trainable_decoder_layers, n_trainable, n_total, epochs, best_epoch, selection, lr, batch_size, max_train_source_tokens, max_train_target_tokens, eval_generation, n_train, n_val, seed, history, trainable_names, seconds}`; sets `pipe.adapter`.
- `save_artifact(directory, metadata=None)` → directory with `adapter.safetensors` (trained tensors only) + `manifest.json` (`format` = `org.valcorza.t5-base.adapter.v1`, base `model_id`/`model_revision`/`weight_sha256`, the `prefix`, `tensors`, `sha256`); `load_artifact(directory)` verifies, applies the tensors in place and returns the manifest; `T5BaseText2TextPipeline.from_artifact(directory, *, device=None, weights_dir=None)` loads the verified base and applies it.
- `metrics.py`: `rouge_n(hyp, ref, n)`, `rouge_l(hyp, ref)`, `rouge_scores(hyp, refs)`, `text_metrics(hyps, refs)`, `lead_sentences(text, n)`, `lead_baseline(records, n_sentences=1)`. `samples.py`: `fetch_corpus(cache_dir=None, fetcher=None)`, `read_corpus(files)`, `filter_records`, `build_sample_dataset(corpus, seed=42, sizes=SAMPLE_SPLIT)`, `fetch_sample_dataset()`, `validate_dataset(records)`, `dataset_digest`, `check_split_disjoint(splits)`, `split_dataset(records, val_fraction=0.15, test_fraction=0.2, seed=0)`, `load_byod_dataset(path)` (CSV / JSON / JSONL), `write_dataset_csv(records, path)`; constant `DEFAULT_PREFIX = "paper title: "`.

## Runtime

- Pins: `torch==2.14.0`, `torchvision==0.29.0`, `torchaudio==2.11.0`, `transformers==4.57.6`, `tokenizers==0.22.2`, `sentencepiece==0.2.2`, `huggingface-hub==0.36.2`, `safetensors==0.8.0`, `numpy==2.5.3`; Python 3.12.
- Precision: float32 on both CPU and CUDA; decoding greedy by default, beam search on request, sampling disabled; upstream `task_specific_params` (min_length, length_penalty, no_repeat_ngram_size) not applied.
- Measured (Windows venv `dimer-next16`, torch 2.14.0+cu130 build, `CUDA_VISIBLE_DEVICES=-1`, `HF_HUB_OFFLINE=1`): device `cpu`, source `local-snapshot`, load + verify 4.55 s; translate 11 → 5 tokens in 0.29 s (greedy) / 0.21 s (4 beams), output `Das Haus ist wunderbar.`; summarise 94 → 40 tokens in 0.83 s, stopped by `eos`; total 5.88 s; no loader warnings.
- Measured, adaptation (same venv and CPU, from the recorded notebook run): corpus read + split < 1 s from cache; frozen test evaluation (100 abstracts, 4 beams, 24 new tokens) 26.8 s under `paper title: ` and 64.9 s under `summarize: `; `adapt` 2 epochs × 300 abstracts, last 4 decoder blocks (37,757,952 / 222,903,552 params) 284.2 s incl. per-epoch validation; adapted test evaluation 51.3 s; adapter 151,037,976 B / 52 tensors; reload parity 6/6; the committed notebook blob `486beac4` (commit `e09a306`) executed 11/11 cells in 467.7 s (pre-flight harness; recorded in `docs/release-verification.md`). Peak CPU RAM was not measured; the weights alone need about 0.9 GB, and training the four blocks adds their gradients and AdamW moments (about 0.45 GB).
- Tests: `pytest -q -o addopts= tests` — 33 passed offline without weights, plus 5 notebook-parity tests (2 further model-backed tests run when the snapshot is staged: `evaluate` under a new prefix, and a 1-epoch adaptation of the last block with an artifact round trip); `ruff check src tests tools` clean.

## References

- Cachola, Lo, Cohan, Weld. TLDR: Extreme Summarization of Scientific Documents. Findings of EMNLP 2020. https://arxiv.org/abs/2004.15011 (SciTLDR-A abstracts and titles, Apache-2.0: https://github.com/allenai/scitldr)
- Lin. ROUGE: A Package for Automatic Evaluation of Summaries. ACL 2004 Workshop. https://aclanthology.org/W04-1013
- Raffel, Shazeer, Roberts, Lee, Narang, Matena, Zhou, Li, Liu. Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer. JMLR 21(140):1–67, 2020. https://arxiv.org/abs/1910.10683
- Dodge, Sap, Marasović, Agnew, Ilharco, Groeneveld, Mitchell, Gardner. Documenting Large Webtext Corpora: A Case Study on the Colossal Clean Crawled Corpus. EMNLP 2021. https://arxiv.org/abs/2104.08758
- Upstream code: https://github.com/google-research/text-to-text-transfer-transformer
- Upstream card: https://huggingface.co/google-t5/t5-base
